/**
 * "나 중심 지도" 관련 튜닝 상수.
 *
 * 서버 전용 의존성(Redis 등)이 전혀 없는 순수 상수 모듈이라 클라이언트
 * 컴포넌트(지도 페이지 등)와 서버(kv.ts, API 라우트) 양쪽에서 그대로
 * import해도 번들에 서버 코드가 섞여 들어가지 않는다.
 */

// 방장이 만들 수 있는 지도 하나에 등록 가능한 친구 최대 인원.
export const MAX_FRIENDS = 100;

// 이 인원 이하는 방사형(별자리) 뷰, 초과하면 리스트 뷰로 자동 전환.
// 잠정치 — 실사용 데이터를 보고 조정할 수 있도록 상수 하나로 분리해둠.
export const RADIAL_VIEW_MAX = 10;

// 닉네임 길이 제한 (trim 후 기준).
export const NICKNAME_MAX_LENGTH = 12;
